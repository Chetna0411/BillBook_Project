/*import { NumbersToMonthPipe } from './numbers-to-month.pipe';

describe('NumbersToMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new NumbersToMonthPipe();
    expect(pipe).toBeTruthy();
  });
});*/

import { NumberToMonthPipe } from './numbers-to-month.pipe';

describe('NumberToMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberToMonthPipe();
    expect(pipe).toBeTruthy();
  });
});

